#!/usr/bin/env sh

if git diff --cached --name-only | grep -Eq '(^|/)\.env($|\.(local|development|production|test)$)|\.(pem|key)$'; then
  echo "Commit blocked: staged secret-bearing file." >&2
  exit 1
fi
