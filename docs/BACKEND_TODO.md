# Greenlight backend TODO

Shared-main handoff tracker, audited 2026-08-15 against the working tree and
pushed commits through `a2aafcf`. Statuses are intentionally conservative:

- `[x] DONE` = pushed and evidenced in git.
- `[~] IN REVIEW` = implemented locally but not committed/pushed.
- `[ ] TODO` = not implemented or not verified.
- `[!] WAITING` = requires an external resource, credential, or owner action.

`uxui/**` is reserved for the frontend teammate. `lib/ocr/**` is reserved for
the OCR teammate. This tracker does not assign or modify either path. User-
supplied `tests/file/*.jpeg` fixtures are present untracked; their contents are
not part of this status.

## Foundation

- [x] **DONE — Root Next.js/TypeScript server foundation and stable envelope.** Path/owner: `app/api/health/**`, root config, `lib/reasoning/**`, `tests/reasoning.test.ts` — backend/reasoning agent. Dependency/blocker: none for the pushed baseline. Evidence/definition of done: pushed in `ef9ff01`; retain Node runtime Route Handlers, safe request IDs, and machine-readable errors; verify with `npm run typecheck`, `npm test`, and `npm run build` on clean main.
- [x] **DONE — Relational schema and Ontario program identities.** Path/owner: `db/migrations/001_initial.sql`, `db/seeds/002_program_identities.sql` — database agent. Dependency/blocker: application-level case smoke tests remain part of the end-to-end flow. Evidence/definition of done: pushed in `71eb12e`; Railway base schema was applied and externally verified with 16 tables, 14 enums, 5 program identities, 6 direct case foreign keys, and 3 composite foreign keys.
- [x] **DONE — Deterministic eligibility and financial primitives.** Path/owner: `lib/eligibility/**`, `lib/financial/**`, `tests/engines.test.ts` — deterministic-engine agent. Dependency/blocker: orchestration must supply reviewed program rules and bill answers. Evidence/definition of done: pushed in `e4936f9`; tests prove unknown/manual-review behavior and keep financing separate from savings.
- [x] **DONE — Authenticated case create/status slice.** Path/owner: `app/api/cases/**`, `lib/auth/**`, `lib/cases/**`, `tests/cases.test.ts` — backend/API agent. Dependency/blocker: database-backed end-to-end smoke coverage remains. Evidence/definition of done: pushed in `80f6aa2`; handlers use Clerk ownership checks and idempotency, and the Clerk app is linked with keys kept only in ignored `.env.local`/Vercel Preview settings.
- [ ] **TODO — Complete PRD case API surface and state orchestration.** Path/owner: `app/api/documents/**`, `app/api/cases/[id]/**`, `app/api/opportunities/**`, `app/api/actions/**` — backend/API agent. Dependency/blocker: OCR contract, reviewed program data, and action routes. Evidence/definition of done: implement fields, answers, result, prepare, and approve routes with the documented envelope and idempotent state transitions.

## Platform

- [x] **DONE — Railway PostgreSQL is provisioned and migrated.** Path/owner: Railway project plus `lib/db/**`/`db/**` — platform/database agents. Dependency/blocker: application-level owned-case smoke coverage and backup/restore confirmation remain. Evidence/definition of done: Railway base schema was applied and externally verified with 16 tables, 14 enums, 5 program identities, 6 direct case foreign keys, and 3 composite foreign keys; public `DATABASE_URL` is configured only in ignored `.env.local` and Vercel Preview settings.
- [x] **DONE — Clerk server authorization adapter.** Path/owner: `lib/auth/**`, `app/api/cases/**`, `.env.example` — backend/API agent. Dependency/blocker: final allowed-origin verification remains. Evidence/definition of done: pushed in `80f6aa2`; server-side `auth()` ownership checks are covered by the authenticated case slice, with Clerk keys kept out of tracked files.
- [x] **DONE — Presigned upload validation and database reservation.** Path/owner: `app/api/uploads/route.ts`, `lib/uploads/**`, `tests/uploads.test.ts` — backend/API agent. Dependency/blocker: real private-S3 PUT/HEAD/checksum smoke test remains after the deployment URL is known. Evidence/definition of done: pushed in `9d0e584`; API validates type, size, checksum, filename, opaque case-scoped keys, TTL, and ownership.
- [x] **DONE — AWS private-upload stack deployed.** Path/owner: `infra/aws/upload-stack.yaml` — AWS infrastructure agent. Dependency/blocker: real upload/checksum smoke test remains before end-to-end use. Evidence/definition of done: CloudFormation is `UPDATE_COMPLETE` for the private AES256 bucket, one-day `cases/` expiration, Vercel OIDC provider/role, and trust for Vercel Preview; S3 CORS was updated for the stable public alias.
- [x] **DONE — OpenRouter provider and web-research reasoning.** Path/owner: `lib/ai/**`, `lib/reasoning/**`, `.env.example`, `package.json` — backend/reasoning agent. Dependency/blocker: approved paid/enterprise terms are still required before real bills; the configured key remains ignored/sensitive. Evidence/definition of done: pushed in `65c0dc1` and `040b355`; the route uses the Vercel AI SDK/OpenRouter provider and labels web citations unreviewed.
- [ ] **TODO — Root UI/API integration contract handoff.** Path/owner: root Route Handlers plus reserved `uxui/**` consumer — coordinating/backend and frontend owners. Dependency/blocker: result and action endpoints are not complete. Evidence/definition of done: document and smoke-test the result payload, status polling, errors, and execution mode against the frontend without editing `uxui/**` here.

## Core pipeline

- [x] **DONE — Official-source fetch, chunking, and retrieval slice.** Path/owner: `data/legal-sources.json`, `lib/ingestion/**`, `lib/retrieval/**`, `tests/ingestion.test.ts` — retrieval/data agent. Dependency/blocker: reviewed program versions/rules/benefits remain; vector ingestion is blocked separately. Evidence/definition of done: full-text ingestion pushed in `17aaffa`; 9/9 official sources loaded into Railway as 88 chunks, all pending review. Full-text retrieval remains intact; vector ingestion attempted for 9/9 sources but succeeded for 0/9, leaving zero vectors.
- [~] **IN REVIEW — pgvector extension, migration, and embeddings.** Path/owner: `db/migrations/002_legal_embeddings.sql`, Railway database — database owner/user action. Dependency/blocker: the user enabled `vector` and applied migration 002 successfully; the database now has 3 embedding columns and an HNSW index. OpenRouter diagnostics for both configured embedding models returned HTTP 401 `User not found` while the auth/key metadata endpoint returned 200, so a fresh OpenRouter key is required before rerunning ingestion and verifying retrieval. Keep the 88 full-text chunks as the MVP fallback.
- [ ] **TODO — Five-program reviewed registry content.** Path/owner: `db/seeds/**`, `data/**`, program-source review workflow — database/product-research owner. Dependency/blocker: current seed creates identities only; official snapshots and human review are missing. Evidence/definition of done: OESP, EAP, LEAP, Home Renovation Savings, and Toronto HELP each have current versioned sources, eligibility rules, benefits, route records, effective dates, and exact evidence excerpts; HELP financing stays out of savings.
- [~] **IN REVIEW — OCR extraction and canonical bill schema.** Path/owner: reserved `lib/ocr/**` plus Textract integration — OCR teammate. Dependency/blocker: the Textract foundation was pushed and unit-tested in `e73043d`, but the live AWS call returned `SubscriptionRequiredException`. The explicit fallback worker is pushed in `a2aafcf`; 27 tests, typecheck, and build pass, but the approved live smoke returned HTTP 401 `AI_APICallError`, with no OCR output or latency. Do not mark this complete until the OpenRouter credential/network blocker is resolved and a latency-bearing live smoke passes. The fallback preserves the shared contract and review-required confidence behavior.
- [ ] **TODO — Document analyze, normalization, and human review.** Path/owner: `app/api/documents/[documentId]/analyze`, `app/api/documents/[id]/fields`, `lib/ocr/**` — backend/API plus OCR owners. Dependency/blocker: verified S3 object, Textract result, OpenRouter normalization via the Vercel AI SDK, and confidence thresholds. Evidence/definition of done: reject magic-byte/size/checksum mismatches, mask account data, persist minimized fields/evidence, and move conflicting or low-confidence critical fields to `needs_review`.
- [ ] **TODO — One-question evaluation loop.** Path/owner: `app/api/cases/[id]/answers`, eligibility orchestration — backend/reasoning plus deterministic-engine owners. Dependency/blocker: normalized bill facts, five-program rules, and case answers. Evidence/definition of done: filter by Ontario/provider/date, calculate deterministic outcomes, and ask only the highest-value unresolved question with no invented probability.

## Evaluation and action

- [ ] **TODO — End-to-end deterministic evaluation and value components.** Path/owner: evaluation service using `lib/eligibility/**`, `lib/financial/**`, `case_program_evaluations`, `value_components` — deterministic-engine/backend owners. Dependency/blocker: reviewed registry and normalized case inputs. Evidence/definition of done: reproducible pass/fail/unknown/manual-review results, source/formula versions, cadence/certainty labels, and net benefit that subtracts upfront costs while excluding financing principal.
- [ ] **TODO — Grounded explanation response.** Path/owner: `lib/reasoning/**`, result route — backend/reasoning agent. Dependency/blocker: deterministic result, reviewed retrieval excerpts, and approved provider terms. Evidence/definition of done: explanation is structured JSON over supplied facts/source IDs, may clarify uncertainty, and cannot alter eligibility, dollars, contacts, or citations.
- [ ] **TODO — Verified action router and approval audit.** Path/owner: `action_routes`, `prepared_actions`, `audit_events`, `/api/opportunities/{id}/prepare`, `/api/actions/{id}/approve` — backend/API agent. Dependency/blocker: reviewed route destinations and Clerk ownership. Evidence/definition of done: support portal/form/phone/mail/intake-agency/email/manual-review routes, mark stale/unverified routes for manual review, and require explicit approval before any external action.
- [ ] **TODO — Result/status contract for the frontend.** Path/owner: `/api/cases/{id}/status`, `/api/cases/{id}/result` — backend/API agent; consumer reserved `uxui/**`. Dependency/blocker: pipeline state transitions and evidence records. Evidence/definition of done: return `caseId`, status, bill fields/evidence, opportunities, values, action route, next question, safe errors, and request IDs consistently in live, hybrid, and demo modes.

## Resilience and security

- [ ] **TODO — Trust-boundary verification and privacy controls.** Path/owner: upload/analyze/OCR/AI routes and server logs — backend/API plus OCR owners. Dependency/blocker: S3 object verification and OCR implementation. Evidence/definition of done: validate magic bytes and checksum after upload, keep S3 private, mask account numbers before persistence/AI, never log raw documents/OCR/secrets, and reject unsupported/multi-page inputs in the MVP.
- [ ] **TODO — Retry-safe processing and recoverable errors.** Path/owner: all mutating Route Handlers, case/document state, `audit_events` — backend/API agent. Dependency/blocker: complete pipeline and idempotency keys. Evidence/definition of done: retries/polling cannot duplicate cases, jobs, evaluations, or actions; terminal errors map to documented codes and retryability without leaking upstream bodies.
- [ ] **TODO — Live, hybrid, and demo parity.** Path/owner: `EXECUTION_MODE`, fixtures, extraction/evaluation/explanation adapters — backend/reasoning plus frontend owners. Dependency/blocker: complete response contract and synthetic/redacted fixture. Evidence/definition of done: all modes exercise the same schemas, deterministic rules, evidence links, and action router; demo fixtures are explicit in metadata/logs and never silently replace a live document.
- [ ] **TODO — Deletion, reset-demo, and retention.** Path/owner: document/case deletion routes, S3 lifecycle, audit records — backend/API plus AWS owners. Dependency/blocker: deployed private bucket and ownership checks. Evidence/definition of done: user can delete case/document data, demo raw uploads expire automatically, reset is auditable, and recovery behavior is documented.

## Deployment and demo

- [~] **IN REVIEW — Vercel Preview deployment and environment wiring.** Path/owner: root app/config and Vercel project — coordinating/platform owner. Dependency/blocker: the public Preview alias is ready, but authenticated case/upload/reasoning smoke and real private-S3 PUT/checksum verification remain. Evidence/definition of done: deployment `https://centennialhacks2026-63z2thkye-daveta.vercel.app` is `READY`; stable API base `https://centennialhacks2026.vercel.app` points to that Preview-built deployment; `/api/health` returned service status `ok`; Vercel SSO protection is disabled; 14 expected environment names are configured without documenting values; AWS CORS was updated for the stable alias and still needs browser verification. The accidental production-target deployment is unused and not part of the demo; production setup is out of scope for this hackathon.
- [x] **DONE — Clean-main verification gate.** Path/owner: root `package.json`, tests, CI/check commands — coordinating agent. Dependency/blocker: rerun after any further material code slice. Evidence/definition of done: pushed OCR fallback commit `a2aafcf` passes 27 tests, `npm run typecheck`, and `npm run build`; no secrets are documented here.
- [ ] **TODO — Under-three-minute stage flow.** Path/owner: root API plus reserved `uxui/**` — coordinating/backend and frontend owners. Dependency/blocker: complete upload-to-action pipeline and a synthetic/redacted bill. Evidence/definition of done: authenticated demo creates a case, uploads/analyzes, shows evidence and deterministic values, asks at most one question, prepares a verified route, and records approval in under three minutes.
- [x] **DONE — Demo runbook and external-state checklist.** Path/owner: [`docs/DEPLOYMENT_RUNBOOK.md`](./DEPLOYMENT_RUNBOOK.md), deployment settings, demo fixture — backend documentation status sync. Dependency/blocker: the runbook records remaining external and product blockers; it does not claim an end-to-end demo until those checks pass. Evidence/definition of done: Preview URL, health check, OCR fallback state, mode/fixture guidance, reset steps, and the concise remaining checklist are documented without secrets.

## Shared-main workflow

1. Claim an unowned path in `docs/OWNERSHIP.md` before editing; do not touch
   `uxui/**` or `lib/ocr/**` unless that owner explicitly reassigns it.
2. Fetch and fast-forward only from a clean worktree. Inspect concurrent
   changes before touching shared files.
3. Implement the smallest slice, run its focused checks, then run the relevant
   typecheck/tests/build. Do not stage another agent's work.
4. Inspect the staged diff and scan for secrets; make one small atomic commit
   and push it normally to `main` (never force-push or rewrite shared history).
5. Mark the slice here with its commit/evidence, dependency state, and next
   blocker so the next agent can pick it up safely.
